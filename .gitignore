node_modules/
dist/
.env
.env.local
out/
*.log
*.tsbuildinfo
.DS_Store

# Editor / agent workspace metadata (per-user, not shared)
.claude/
.vscode/
.idea/
.entire/

# apps/landing primarily uses Bun (bun.lockb is committed); the npm
# package-lock.json only appears if a contributor runs `npm install`
# inside apps/landing. Don't commit it — would create a second source
# of truth for the dep tree.
apps/landing/package-lock.json

# Pipeline output + caches
runs/
.cache/
.peec_oauth.json
**/.peec_oauth.json

# Python (legacy — being ported to TS in P2)
.venv/
__pycache__/
*.pyc
*.egg-info/
peec-onboarder/data/

# Secrets — never commit, anywhere in the tree
**/.env
**/.env.local
