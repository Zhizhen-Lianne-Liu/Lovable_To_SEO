// P2: port from src/peec/client.ts (already clean) + domain-peec-enrichment/py/research/{push,snapshot}.py
// Endpoints: /brands, /prompts, /models, /chats, /chats/{id}/content,
// /reports/brands, /reports/domains, /reports/urls, /search_queries.

export const _placeholder = true;
