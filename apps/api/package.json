{
  "name": "@lovabletoseo/api",
  "version": "0.1.0",
  "description": "Demo Hono server for the lovabletoseo landing. Returns the baked example run for any URL — keeps judges able to paste a domain and see real-shape output without running the full pipeline.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
